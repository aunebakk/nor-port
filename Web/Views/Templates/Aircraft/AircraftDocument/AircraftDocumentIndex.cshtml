<!--
  SQL2X Generated code based on a SQL Server Schema
  SQL2X Version: 1.0
  http://sql2x.org/
  Generated Date: 1/6/2021 4:51:11 PM
  From Machine: DESKTOP-LSRVP12
  Template: sql2x.TemplateWithDurianGenerator.ParentViewIndex
-->
@model IEnumerable<SolutionNorSolutionPort.BusinessLogicLayer.AircraftDocumentIndexWithFilterContract>

@{
    ViewBag.Title = "Index Aircraft Document";
}

<h2>Index for Aircraft Document</h2>

<p>
    @Html.ActionLink("Create New", "AircraftDocumentCreate", new { aircraftId = ViewBag.AircraftId })
</p>

<table class="table">
    <tr>
        <th>Aircraft Name</th>
        <th>Aircraft Document Type Name</th>
        <th>Document Name</th>
        <th>Default User Name</th>
        <th>Engine Type</th>
        <th>Aircraft Document Type</th>
        <th>Document Date Time</th>
        <th>Expiry Date Time</th>
        <th>Date Time</th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.AircraftName) </td>
            <td>@Html.DisplayFor(modelItem => item.AircraftDocumentTypeName) </td>
            <td>@Html.DisplayFor(modelItem => item.DocumentName) </td>
            <td>@Html.DisplayFor(modelItem => item.DefaultUserName) </td>
            <td>@Html.DisplayFor(modelItem => item.EngineType) </td>
            <td>@Html.DisplayFor(modelItem => item.AircraftDocumentTypeRcd) </td>
            <td>@Html.DisplayFor(modelItem => item.DocumentDateTime) </td>
            <td>@Html.DisplayFor(modelItem => item.ExpiryDateTime) </td>
            <td>@Html.DisplayFor(modelItem => item.DateTime) </td>
            <td>
                @Html.ActionLink("Edit", "AircraftDocumentEdit", new { aircraftDocumentId = item.AircraftDocumentId })
            </td>
        </tr>
    }

</table>
