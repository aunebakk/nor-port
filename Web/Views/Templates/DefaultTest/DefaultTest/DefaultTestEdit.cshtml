<!--
  SQL2X Generated code based on a SQL Server Schema
  SQL2X Version: 1.0
  http://sql2x.org/
  Generated Date: 1/3/2021 4:16:02 AM
  From Machine: DESKTOP-LSRVP12
  Template: sql2x.TemplateByServiceTableCrudGenerator.ParentViewEdit
-->
@model SolutionNorSolutionPort.BusinessLogicLayer.DefaultTestContract

@{
    ViewBag.Title = "Edit Default Test";
}

<br />
@Html.Partial("~/Views/Templates/DefaultTest/_tab.cshtml")
<br />

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ChecksumAfterGet)

        @for (int i = 0; i < @Model.DefaultTest.Count; i++) {
            @Html.HiddenFor(model => model.DefaultTest[i].DefaultTestId)
            @Html.HiddenFor(model => model.DefaultTest[i].UserId)
            @Html.HiddenFor(model => model.DefaultTest[i].DateTime)
        }

    </div>
    <h3>Default Test</h3>
    <div>
        @Html.ActionLink("refresh", "DefaultTestIndex", "DefaultTest") | 
        @Html.ActionLink("add new", "DefaultTestCreate", "DefaultTest")
    </div>
    <hr />
    <table class="table">
        <tr>
            <th>System Name</th>
            <th>Test Area</th>
            <th>Test Sub Area</th>
            <th>Test Address</th>
            <th></th>
        </tr>

        @for (int i = 0; i < @Model.DefaultTest.Count; i++) {
            <tr>
                <td>@Html.EditorFor(modelItem => Model.DefaultTest[i].SystemName, new { htmlAttributes = new { @class = "form-control" } })</td>
                <td>@Html.EditorFor(modelItem => Model.DefaultTest[i].TestArea, new { htmlAttributes = new { @class = "form-control" } })</td>
                <td>@Html.EditorFor(modelItem => Model.DefaultTest[i].TestSubArea, new { htmlAttributes = new { @class = "form-control" } })</td>
                <td>@Html.EditorFor(modelItem => Model.DefaultTest[i].TestAddress, new { htmlAttributes = new { @class = "form-control" } })</td>
            </tr>
        }

    </table>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn-primary" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Styles.Render("~/Content/themes/base/all.css")
    <script type="text/javascript">
        $(function () {
            // This will make every element with the class "date-picker" into a DatePicker element
            $('.date-picker').datepicker(({ dateFormat: "yy-mm-dd" }));
        })
    </script>
}
